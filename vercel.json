{
  "name": "comment",
  "github": { "silent": true },
  "installCommand": "corepack enable && corepack prepare pnpm@10.28.2 --activate && pnpm install --no-frozen-lockfile",
  "builds": [
    { "src": "example/index.cjs", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "example/index.cjs" }
  ]
}
